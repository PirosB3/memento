# Test environment — uses a separate database that can be wiped freely.
# This file is committed to the repo. Do NOT put secrets here.
DATABASE_URL="postgresql://summon:summon@localhost:5432/summon_test"
